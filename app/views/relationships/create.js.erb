var btn = $('[pos-id="<%= @member.id %>"]');
btn.replaceWith('<%= j(link_to(relationship_path(@member),
  remote: true,
  method: 'delete',
  'pos-id': @member.id,
  class: 'follow-red'
) { ('<i class="fas fa-heart"></i>').html_safe }) %>')
