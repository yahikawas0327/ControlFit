<%= form_for(current_member.active_relationships.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :followed_id, @member.id %></div>
  <%= f.submit "Follow", class: "btn btn-primary" %>
<% end %>